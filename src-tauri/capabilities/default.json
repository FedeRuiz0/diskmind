{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop permissions for DiskMind bootstrap.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
